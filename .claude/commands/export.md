---
description: Export the current conversation
argument-hint: "[filename]"
---

Export the current conversation to a file.

$ARGUMENTS

Exports the full conversation history including:
- User messages
- Assistant responses
- Tool usage (if verbose mode)
- Timestamps

Default format: Markdown
Default filename: conversation-{timestamp}.md
