---
description: Initialize a new project with CLAUDE.md
argument-hint: "[project-name]"
---

Initialize a new project with CLAUDE.md configuration.

$ARGUMENTS

This will:
1. Create a CLAUDE.md file with project configuration
2. Set up recommended project structure
3. Configure default settings for the project

If a CLAUDE.md already exists, show its contents instead.
